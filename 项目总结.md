# 租赁合同管理系统 (Lease Contract)

## 项目概述

一个完整的租赁合同管理解决方案，包含微信小程序客户端和 Node.js 后端服务，支持合同创建、签署、管理全流程。

**技术栈**: Node.js + Express + TypeScript | 微信小程序 | MySQL/SQLite

---

## 项目结构

```
d:\Pro\Lease_Contract\
├── backend/                 # 后端服务
│   ├── src/
│   │   ├── config/         # 配置文件
│   │   ├── controllers/    # 控制器层
│   │   ├── middleware/      # 中间件 (认证、CORS)
│   │   ├── models/          # 数据模型
│   │   ├── routes/          # 路由定义
│   │   ├── services/        # 业务服务 (PDF生成)
│   │   ├── app.ts           # 应用入口
│   │   └── database.ts      # 数据库连接
│   ├── config/
│   │   └── .env             # 环境配置
│   ├── data/                # SQLite 数据文件
│   ├── uploads/             # 上传文件目录
│   ├── dist/                # 编译输出
│   └── package.json
│
├── miniprogram/            # 微信小程序
│   ├── pages/              # 页面
│   │   ├── index/          # 首页
│   │   ├── login/           # 登录
│   │   ├── real-name/       # 实名认证
│   │   ├── contracts/       # 合同列表
│   │   ├── my-contracts/    # 我的合同
│   │   ├── create-contract/ # 创建合同
│   │   ├── sign-contract/   # 签署合同
│   │   ├── contract-detail/ # 合同详情
│   │   ├── profile/          # 个人中心
│   │   └── verify-invite/   # 验证邀请
│   ├── components/          # 组件
│   ├── services/           # API 服务
│   ├── utils/              # 工具函数
│   └── package.json
│
├── lease_contract_template.html  # 合同HTML模板
└── database_schema.md           # 数据库设计文档
```

---

## 功能模块

### 1. 用户模块

| 功能 | 说明 |
|------|------|
| 手机号登录 | 支持短信验证码登录 |
| 微信登录 | 微信小程序授权登录 |
| 实名认证 | 身份证信息认证 |
| 角色切换 | 甲方(房东)/乙方(租客) |

### 2. 合同管理

| 功能 | 说明 |
|------|------|
| 创建合同 | 填写甲方、乙方、房屋、租金等信息 |
| 合同列表 | 查看所有合同，支持状态筛选 |
| 合同详情 | 查看合同完整信息 |
| 删除合同 | 删除草稿或已取消合同 |
| 状态流转 | 草稿 → 待签署 → 已签署 → 已到期 |

### 3. 电子签名

| 功能 | 说明 |
|------|------|
| 甲方签署 | 房东签名确认 |
| 乙方签署 | 租客签名确认 |
| 签名验证 | 记录签名时间、IP、设备信息 |
| 签署邀请 | 生成分享链接/二维码邀请签署 |

### 4. 支付管理

| 功能 | 说明 |
|------|------|
| 支付方式 | 押一付一、押一付三、押一付六、年付 |
| 分期付款 | 支持多次付款计划 |
| 押金管理 | 押金金额及退还规则 |

### 5. PDF 生成

- HTML 模板渲染
- PDFKit PDF 文档生成
- 中文字体支持

---

## 数据库设计

### 核心表结构

**users** - 用户表
- openid, phone, name, idcard, 实名认证状态, 角色

**contracts** - 合同表
- 合同编号、甲方/乙方信息、房屋信息、租赁期限、租金支付、费用约定、签署状态

**signatures** - 签名表
- 合同ID、签名类型、签名数据、签名时间、IP、设备信息

**sign_invitations** - 签署邀请表
- 邀请码、状态、过期时间

**contract_inventory** - 物品清单表
**contract_payments** - 付款记录表

详见: [database_schema.md](database_schema.md)

---

## API 接口

### 用户接口 `/api/users`
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /send-code | 发送验证码 |
| POST | /phone-login | 手机号登录 |
| POST | /wechat-login | 微信登录 |
| GET | /profile | 获取用户资料 |
| PUT | /profile | 更新用户资料 |
| POST | /real-name/verify | 实名认证 |

### 合同接口 `/api/contracts`
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | / | 获取合同列表 |
| POST | / | 创建合同 |
| GET | /:id | 获取合同详情 |
| DELETE | /:id | 删除合同 |
| POST | /:id/sign | 甲方签署 |
| POST | /:id/tenant-sign | 乙方签署 |
| POST | /:id/reject | 拒绝合同 |
| POST | /:id/share | 生成分享链接 |
| GET | /invite-qrcode/:code | 获取邀请二维码 |

### 其他接口
- `/api/templates` - 合同模板
- `/api/invitations` - 签署邀请
- `/api/signatures` - 签名记录
- `/api/upload` - 文件上传
- `/api/notifications` - 通知

---

## 技术架构

### 后端 (Node.js + Express + TypeScript)

```
┌─────────────────────────────────────────┐
│              Express Server              │
├─────────────────────────────────────────┤
│  Middleware: Auth, CORS, Validation     │
├─────────────────────────────────────────┤
│  Routes: users, contracts, templates... │
├─────────────────────────────────────────┤
│  Controllers: Business Logic            │
├─────────────────────────────────────────┤
│  Services: PDF Generation, HTML Render  │
├─────────────────────────────────────────┤
│  Database: MySQL / SQLite              │
└─────────────────────────────────────────┘
```

**核心依赖**:
- express: Web 框架
- typescript: 类型支持
- better-sqlite3: SQLite 驱动
- mysql2: MySQL 驱动
- jsonwebtoken: JWT 认证
- pdfkit: PDF 生成
- multer: 文件上传

### 前端 (微信小程序)

```
┌─────────────────────────────────────────┐
│           WeChat Mini Program           │
├─────────────────────────────────────────┤
│  Pages: 首页、合同、我的、登录、实名...   │
├─────────────────────────────────────────┤
│  Components: @vant/weapp 组件库         │
├─────────────────────────────────────────┤
│  API Service: 请求封装、统一错误处理     │
└─────────────────────────────────────────┘
```

**核心依赖**:
- @vant/weapp: Vant 微信小程序组件库

---

## 环境配置

### 后端配置 (backend/config/.env)

```env
PORT=3000
NODE_ENV=development
DB_TYPE=sqlite
SQLITE_PATH=./data/lease_contract.db

# MySQL (可选)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=lease_contract
```

### 启动后端服务

```bash
cd backend
npm install
npm run dev    # 开发模式 (热重载)
npm run build  # 编译 TypeScript
npm start      # 生产模式
```

---

## 合同状态流转

```
                    ┌──────────┐
                    │   草稿   │
                    └────┬─────┘
                         │ 发送签署
                         ▼
              ┌──────────────────────┐
              │    待甲方签署         │
              └──────────┬───────────┘
                         │ 甲方签署
                         ▼
              ┌──────────────────────┐
              │    待乙方签署         │
              └──────────┬───────────┘
                         │ 乙方签署
                         ▼
                    ┌──────────┐
                    │   已签署  │
                    └────┬─────┘
                         │ 到期
                         ▼
                    ┌──────────┐
                    │   已到期  │
                    └──────────┘

中途可拒绝或取消:
  待甲方签署 ──取消──→ 已取消
  待乙方签署 ──拒绝──→ 已拒绝
  待乙方签署 ──取消──→ 已取消
```

---

## 微信小程序页面说明

| 页面 | 功能 |
|------|------|
| 首页 | 首页概览、快捷入口 |
| 登录 | 手机号/微信登录 |
| 实名认证 | 提交身份证信息 |
| 合同列表 | 浏览、搜索合同 |
| 我的合同 | 当前用户合同管理 |
| 创建合同 | 填写合同信息向导 |
| 签署合同 | 手写签名、确认签署 |
| 合同详情 | 查看合同完整内容 |
| 个人中心 | 头像、信息管理 |

---

## 项目特点

1. **双端支持**: 微信小程序 + 后端 API 服务
2. **多数据库**: 支持 MySQL 和 SQLite 可切换
3. **电子签名**: 支持甲方乙方分别签名
4. **邀请签署**: 生成分享链接/二维码
5. **PDF导出**: 支持生成合同 PDF 文档
6. **状态管理**: 完整的合同状态流转
7. **物品清单**: 房屋物品记录功能
8. **分期付款**: 灵活的支付计划

---

## 开发说明

### 目录权限
- `backend/uploads/` - 上传文件目录，需有写权限
- `backend/data/` - SQLite 数据库目录

### 注意事项
- 小程序需配置合法域名后方可正式使用
- 生产环境建议使用 MySQL 数据库
- 签名图片为 Base64 格式存储
- 邀请链接有效期可配置

---

*文档生成时间: 2026-03-24*
